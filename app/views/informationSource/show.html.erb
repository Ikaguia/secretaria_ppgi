<!-- <p id="notice"><%= notice %></p> -->

<p>
  <strong>Nome:</strong>
  <%= @informationSource.name %>
</p>

<% if user_signed_in? and current_user.role == 'administrator' %>
	<%= link_to 'Alterar', edit_informationSource_path(@informationSource) %> |
<% else %>
	<h1>Você não tem permissão para acessar essa página</h1>
<% end %>
<%= link_to 'Voltar', informationSource_index_path %>
