<% if user_signed_in? and current_user.role == 'administrator' %>
	<h1>Criando Fontes de Informação</h1>

	<%= render 'form', informationSource: @informationSource %>
<% else %>
	<h1>Você não tem permissão para acessar essa página</h1>
<% end %>

<%= link_to 'Voltar', informationSouce_index_path %>

